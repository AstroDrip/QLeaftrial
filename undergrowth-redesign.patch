diff --git a/apps/api/package.json b/apps/api/package.json
index e942c1e..a2aeedb 100644
--- a/apps/api/package.json
+++ b/apps/api/package.json
@@ -19,15 +19,17 @@
   "dependencies": {
     "@prisma/client": "6.16.2",
     "argon2": "0.44.0",
+    "compression": "^1.7.4",
     "express": "^5.1.0",
     "zod": "^3.24.1"
   },
   "devDependencies": {
-    "@types/express": "^5.0.3",
+    "@types/compression": "^1.8.1",
+    "@types/express": "^5.0.6",
     "@types/node": "^24.3.0",
     "@types/supertest": "^6.0.3",
-    "supertest": "^7.1.4",
     "prisma": "6.16.2",
+    "supertest": "^7.1.4",
     "tsx": "^4.20.5",
     "typescript": "^5.9.2",
     "vitest": "^3.2.4"
diff --git a/apps/api/src/app.ts b/apps/api/src/app.ts
index 7487834..baa3bc3 100644
--- a/apps/api/src/app.ts
+++ b/apps/api/src/app.ts
@@ -1,10 +1,13 @@
 import express, { type Express } from "express";
+import compression from "compression";
 import { errorHandler } from "./middleware/error-handler.js";
 import { apiRouter } from "./routes.js";
 
 export function createApp(): Express {
   const app = express();
 
+  // Enable gzip compression for responses > 1KB
+  app.use(compression({ threshold: 1024 }));
   app.use(express.json());
   app.get("/api/v1/health", (_request, response) => {
     response.json({ status: "ok" });
diff --git a/apps/api/src/modules/products/product.routes.ts b/apps/api/src/modules/products/product.routes.ts
index b4b30fa..a9b016b 100644
--- a/apps/api/src/modules/products/product.routes.ts
+++ b/apps/api/src/modules/products/product.routes.ts
@@ -1,11 +1,12 @@
 import { Router } from "express";
 import { ApiError } from "../../middleware/error-handler.js";
+import { cacheProductList, cacheProductDetail } from "../../middleware/cache-headers.js";
 import { productQuerySchema } from "./product.schemas.js";
 import { findProductBySlug, listProducts } from "./product.service.js";
 
 export const productRouter = Router();
 
-productRouter.get("/products", async (request, response) => {
+productRouter.get("/products", cacheProductList, async (request, response) => {
   const parsedQuery = productQuerySchema.safeParse(request.query);
 
   if (!parsedQuery.success) {
@@ -15,7 +16,7 @@ productRouter.get("/products", async (request, response) => {
   response.json(await listProducts(parsedQuery.data));
 });
 
-productRouter.get("/products/:slug", async (request, response) => {
+productRouter.get("/products/:slug", cacheProductDetail, async (request, response) => {
   const product = await findProductBySlug(request.params.slug);
 
   if (!product) {
diff --git a/apps/web/src/features/catalog/CatalogPage.tsx b/apps/web/src/features/catalog/CatalogPage.tsx
index 3ec1e8b..f9ef075 100644
--- a/apps/web/src/features/catalog/CatalogPage.tsx
+++ b/apps/web/src/features/catalog/CatalogPage.tsx
@@ -229,6 +229,7 @@ export function CatalogPage() {
                 <li key={product.id} className="product-card">
                   <img
                     src={product.image?.url ?? "/images/hero/leaf-1.svg"}
+                    loading="lazy"
                     alt={product.image?.altText ?? ""}
                     className="product-card__image"
                   />
diff --git a/apps/web/src/features/catalog/ProductPage.tsx b/apps/web/src/features/catalog/ProductPage.tsx
index 09d94eb..fe22b7d 100644
--- a/apps/web/src/features/catalog/ProductPage.tsx
+++ b/apps/web/src/features/catalog/ProductPage.tsx
@@ -58,6 +58,7 @@ export function ProductPage() {
       {primaryImage ? (
         <img
           src={primaryImage.url}
+          loading="eager"
           alt={primaryImage.altText}
           className="product-detail__image"
           data-testid="product-image"
diff --git a/apps/web/src/features/catalog/catalog.css b/apps/web/src/features/catalog/catalog.css
index 67cfc04..c777273 100644
--- a/apps/web/src/features/catalog/catalog.css
+++ b/apps/web/src/features/catalog/catalog.css
@@ -1,34 +1,61 @@
-/* Catalog and product-detail styles */
+/* Catalog — Undergrowth editorial design system */
 .catalog {
   display: flex;
   flex-direction: column;
   gap: var(--q-space-lg);
-  padding: var(--q-space-xl) 0;
+  padding: clamp(3rem, 8vw, 5rem) clamp(1.25rem, 5vw, 3.5rem);
+  background: var(--q-color-background);
 }
 
 .catalog__title {
-  font-size: var(--q-font-size-3xl);
+  font-family: var(--q-font-display);
+  font-size: clamp(2.2rem, 5vw, 3.5rem);
+  font-weight: 500;
+  margin: 0;
+  letter-spacing: -0.01em;
+  color: var(--q-color-on-background);
 }
 
+/* Search & Filter Bar */
 .catalog__search {
   display: flex;
   gap: var(--q-space-sm);
   align-items: center;
+  margin-bottom: var(--q-space-lg);
 }
 
 .catalog__search input {
   flex: 1;
-  padding: var(--q-space-sm);
+  padding: 0.7rem 1rem;
+  background: var(--q-color-warm-ivory);
   border: 1px solid var(--q-color-border);
-  border-radius: var(--q-radius-md);
+  border-radius: 0;
+  font-family: var(--q-font-body);
+  font-size: var(--q-font-size-base);
+  color: var(--q-color-on-background);
+}
+
+.catalog__search input::placeholder {
+  color: rgba(22, 31, 24, 0.5);
 }
 
 .catalog__search button {
-  padding: var(--q-space-sm) var(--q-space-md);
-  background: var(--q-color-forest-olive);
+  padding: 0.7rem 1.2rem;
+  background: var(--q-color-terracotta);
   color: var(--q-color-warm-ivory);
   border: none;
-  border-radius: var(--q-radius-md);
+  border-radius: 0;
+  font-family: var(--q-font-body);
+  font-weight: 700;
+  text-transform: uppercase;
+  letter-spacing: 0.08em;
+  font-size: 0.75rem;
+  cursor: pointer;
+  transition: background 200ms ease;
+}
+
+.catalog__search button:hover {
+  background: #9d5133;
 }
 
 .catalog__filters {
@@ -36,59 +63,185 @@
   flex-wrap: wrap;
   gap: var(--q-space-md);
   align-items: flex-end;
+  padding: var(--q-space-md) 0;
+  border-top: 1px solid var(--q-color-border);
+  border-bottom: 1px solid var(--q-color-border);
+  margin-bottom: var(--q-space-lg);
 }
 
 .catalog__filters label {
   display: flex;
   flex-direction: column;
-  gap: var(--q-space-xs);
+  gap: 0.3rem;
   font-size: var(--q-font-size-sm);
+  font-weight: 600;
+  text-transform: uppercase;
+  letter-spacing: 0.08em;
+  color: var(--q-color-on-background);
 }
 
 .catalog__filters select {
-  padding: var(--q-space-xs);
+  padding: 0.5rem 0.7rem;
+  background: var(--q-color-background);
   border: 1px solid var(--q-color-border);
-  border-radius: var(--q-radius-sm);
-  background: var(--q-color-surface);
+  border-radius: 0;
+  font-family: var(--q-font-body);
+  font-size: var(--q-font-size-base);
+  color: var(--q-color-on-background);
+  cursor: pointer;
 }
 
+.catalog__filters button {
+  padding: 0.5rem 1rem;
+  background: transparent;
+  border: 1px solid var(--q-color-border);
+  border-radius: 0;
+  font-family: var(--q-font-body);
+  font-weight: 600;
+  font-size: 0.75rem;
+  text-transform: uppercase;
+  letter-spacing: 0.08em;
+  cursor: pointer;
+  color: var(--q-color-on-background);
+  transition: background 200ms ease;
+}
+
+.catalog__filters button:hover {
+  background: var(--q-color-soft-sand);
+}
+
+/* Product Grid — Editorial Layout */
 .catalog__grid {
   display: grid;
-  grid-template-columns: repeat(auto-fill, minmax(14rem, 1fr));
-  gap: var(--q-space-xl);
+  grid-template-columns: repeat(4, 1fr);
+  gap: 2px;
+  background: var(--q-color-border);
+  border: 1px solid var(--q-color-border);
+  list-style: none;
+  margin: 0;
+  padding: 0;
 }
 
+@media (max-width: 1200px) {
+  .catalog__grid {
+    grid-template-columns: repeat(3, 1fr);
+  }
+}
+
+@media (max-width: 768px) {
+  .catalog__grid {
+    grid-template-columns: repeat(2, 1fr);
+  }
+}
+
+@media (max-width: 520px) {
+  .catalog__grid {
+    grid-template-columns: 1fr;
+  }
+}
+
+/* Product Card — Frame-based */
 .product-card {
   display: flex;
   flex-direction: column;
-  gap: var(--q-space-sm);
-  background: var(--q-color-surface);
-  color: var(--q-color-on-surface);
-  border: 1px solid var(--q-color-border);
-  border-radius: var(--q-radius-lg);
-  padding: var(--q-space-md);
-  text-align: center;
+  background: var(--q-color-background);
+  padding: 1rem 1rem 1.3rem;
+  list-style: none;
+  margin: 0;
 }
 
 .product-card__image {
   width: 100%;
-  height: 10rem;
-  object-fit: contain;
+  aspect-ratio: 4 / 5;
+  object-fit: cover;
+  background: var(--q-color-soft-sand);
+  margin-bottom: 1rem;
+  display: block;
+  transition: transform 1s cubic-bezier(0.16, 0.84, 0.44, 1);
+}
+
+.product-card:hover .product-card__image {
+  transform: scale(1.05);
 }
 
 .product-card__name {
-  font-size: var(--q-font-size-lg);
+  font-family: var(--q-font-display);
+  font-size: 1.15rem;
+  font-weight: 500;
+  margin: 0 0 0.4rem;
+  color: var(--q-color-on-background);
+}
+
+.product-card__price {
+  font-family: var(--q-font-display);
+  font-size: 0.95rem;
+  font-weight: 600;
+  margin: 0;
+  color: var(--q-color-forest-olive);
 }
 
 .product-card__link {
+  margin-top: auto;
+  padding-top: 1rem;
+  border-top: 1px solid var(--q-color-border);
+  color: var(--q-color-terracotta);
+  font-weight: 700;
+  font-size: 0.8rem;
+  text-transform: uppercase;
+  letter-spacing: 0.08em;
+  text-decoration: none;
+  transition: color 200ms ease;
+}
+
+.product-card__link:hover {
+  color: #9d5133;
+}
+
+/* Empty State & Error */
+.catalog__error,
+.catalog__loading,
+.catalog__empty {
+  text-align: center;
+  padding: var(--q-space-2xl) var(--q-space-md);
+  font-size: var(--q-font-size-lg);
   color: var(--q-color-forest-olive);
-  font-weight: 600;
 }
 
+/* Pagination */
 .catalog__pagination {
   display: flex;
   justify-content: center;
   align-items: center;
   gap: var(--q-space-md);
-  padding: var(--q-space-md) 0;
+  padding: var(--q-space-lg) 0;
+}
+
+.catalog__pagination button {
+  padding: 0.6rem 1rem;
+  background: var(--q-color-background);
+  border: 1px solid var(--q-color-border);
+  border-radius: 0;
+  font-family: var(--q-font-body);
+  font-weight: 600;
+  font-size: 0.75rem;
+  text-transform: uppercase;
+  letter-spacing: 0.08em;
+  cursor: pointer;
+  color: var(--q-color-on-background);
+  transition: background 200ms ease;
+}
+
+.catalog__pagination button:hover:not(:disabled) {
+  background: var(--q-color-soft-sand);
+}
+
+.catalog__pagination button:disabled {
+  opacity: 0.4;
+  cursor: not-allowed;
+}
+
+.catalog__pagination span {
+  font-size: var(--q-font-size-sm);
+  color: var(--q-color-forest-olive);
+  font-weight: 600;
 }
diff --git a/apps/web/src/features/catalog/product-detail.css b/apps/web/src/features/catalog/product-detail.css
index b293eda..7d9835a 100644
--- a/apps/web/src/features/catalog/product-detail.css
+++ b/apps/web/src/features/catalog/product-detail.css
@@ -1,55 +1,156 @@
-/* Product detail styles */
+/* Product Detail — Undergrowth editorial design */
 .product-detail {
   display: grid;
-  grid-template-columns: 1fr 1fr;
-  gap: var(--q-space-2xl);
+  grid-template-columns: 1.1fr 0.9fr;
+  gap: clamp(2rem, 5vw, 4rem);
   align-items: start;
-  padding: var(--q-space-xl) 0;
+  padding: clamp(3rem, 8vw, 5rem) clamp(1.25rem, 5vw, 3.5rem);
+  background: var(--q-color-background);
 }
 
+@media (max-width: 900px) {
+  .product-detail {
+    grid-template-columns: 1fr;
+  }
+}
+
+/* Product Image — Full-width frame */
 .product-detail__image {
   width: 100%;
-  aspect-ratio: 1 / 1;
-  object-fit: contain;
-  background: var(--q-color-surface);
-  border: 1px solid var(--q-color-border);
-  border-radius: var(--q-radius-lg);
+  aspect-ratio: 4 / 5;
+  object-fit: cover;
+  background: var(--q-color-soft-sand);
+  border: none;
+  border-radius: 0;
+  display: block;
 }
 
+/* Product Metadata Section */
 .product-detail__meta {
   display: flex;
   flex-direction: column;
-  gap: var(--q-space-sm);
+  gap: 1.2rem;
 }
 
 .product-detail__name {
-  font-size: var(--q-font-size-3xl);
+  font-family: var(--q-font-display);
+  font-size: clamp(1.8rem, 3.5vw, 2.5rem);
+  font-weight: 500;
+  margin: 0;
+  line-height: 1.1;
+  letter-spacing: -0.01em;
+  color: var(--q-color-on-background);
 }
 
 .product-detail__price {
-  font-size: var(--q-font-size-lg);
+  font-family: var(--q-font-display);
+  font-size: 1.4rem;
   font-weight: 600;
-  color: var(--q-color-moss);
+  margin: 0;
+  color: var(--q-color-forest-olive);
 }
 
+/* Stock Status */
 .product-detail__stock {
-  font-weight: 600;
+  font-weight: 700;
+  font-size: 0.9rem;
+  text-transform: uppercase;
+  letter-spacing: 0.08em;
+  padding: 0.5rem 0;
+}
+
+.product-detail__stock[data-testid="in-stock"] {
+  color: var(--q-color-forest-olive);
 }
 
+.product-detail__stock[data-testid="out-of-stock"] {
+  color: #a0603a;
+}
+
+/* Description */
 .product-detail__description {
-  color: var(--q-color-sage);
+  font-size: 0.95rem;
+  line-height: 1.65;
+  color: rgba(22, 31, 24, 0.75);
+  max-width: 48ch;
+  margin: 0;
+  padding: 1rem 0;
+  border-top: 1px solid var(--q-color-border);
+  border-bottom: 1px solid var(--q-color-border);
 }
 
+/* Care Facts / Specs */
 .product-detail__facts {
   list-style: none;
   display: flex;
   flex-direction: column;
-  gap: var(--q-space-xs);
+  gap: 0.7rem;
   padding: 0;
   margin: 0;
 }
 
+.product-detail__facts li {
+  font-size: 0.9rem;
+  line-height: 1.4;
+  color: rgba(22, 31, 24, 0.8);
+  display: flex;
+  gap: 0.5rem;
+}
+
 .product-detail__facts li span {
-  font-weight: 600;
-  color: var(--q-color-on-surface);
+  font-weight: 700;
+  color: var(--q-color-on-background);
+  min-width: 80px;
+}
+
+/* Loading & Error States */
+.product-detail[data-testid="product-loading"],
+.product-detail[data-testid="product-error"],
+.product-detail[data-testid="product-not-found"] {
+  min-height: 50vh;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  text-align: center;
+  flex-direction: column;
+  gap: 1.5rem;
+}
+
+.product-detail[data-testid="product-error"] p,
+.product-detail[data-testid="product-not-found"] p {
+  font-size: 1rem;
+  color: var(--q-color-forest-olive);
+  margin: 0;
+}
+
+.product-detail button {
+  padding: 0.6rem 1.2rem;
+  background: var(--q-color-terracotta);
+  color: var(--q-color-warm-ivory);
+  border: none;
+  border-radius: 0;
+  font-family: var(--q-font-body);
+  font-weight: 700;
+  text-transform: uppercase;
+  letter-spacing: 0.08em;
+  font-size: 0.75rem;
+  cursor: pointer;
+  transition: background 200ms ease;
+}
+
+.product-detail button:hover {
+  background: #9d5133;
+}
+
+.product-detail a {
+  color: var(--q-color-terracotta);
+  font-weight: 700;
+  text-decoration: none;
+  text-transform: uppercase;
+  letter-spacing: 0.08em;
+  font-size: 0.75rem;
+}
+
+.product-detail a:hover {
+  color: #9d5133;
 }
diff --git a/apps/web/src/features/home/HomePage.tsx b/apps/web/src/features/home/HomePage.tsx
index 5472622..bf9a529 100644
--- a/apps/web/src/features/home/HomePage.tsx
+++ b/apps/web/src/features/home/HomePage.tsx
@@ -38,37 +38,70 @@ export function HomePage() {
 
   return (
     <article className="home" ref={containerRef} data-testid="home-page">
-      <header className="hero-section">
-        <p className="hero-section__eyebrow home__wordmark">QLeaves</p>
+      <header className="hero-section home__hero">
+        <div className="hero-section__top">
+          <span className="hero-section__mark home__wordmark">{content.brand}</span>
+          <span className="hero-section__tag">{content.tagline}</span>
+        </div>
 
-        <div className="hero-metric home__count-wrap">
-          <span className="hero-metric__number home__count" aria-live="polite">
-            {availablePlants}
-          </span>
-          <span className="hero-metric__label home__count-note">
-            Plants Remaining
-          </span>
+        <div className="hero-section__leaves" aria-hidden="true">
+          <img src="/images/hero/leaf-1.svg" className="hero-leaf hero-leaf--1" alt="" />
+          <img src="/images/hero/leaf-2.svg" className="hero-leaf hero-leaf--2" alt="" />
+          <img src="/images/hero/leaf-3.svg" className="hero-leaf hero-leaf--3" alt="" />
         </div>
 
-        <h1 className="hero-section__headline home__headline" data-testid="hero-headline">
-          {content.home.heroTitle}
-        </h1>
-        <p className="hero-section__subhead home__subhead" data-testid="hero-subhead">
-          {content.home.heroSubtitle}
-        </p>
-        <Link
-          to="/shop"
-          className="hero-section__cta home__cta"
-          data-testid="hero-cta"
-        >
-          {content.home.shopCta}
-        </Link>
+        <div className="hero-section__mid">
+          <div className="hero-metric home__count-wrap">
+            <span className="hero-metric__number home__count" aria-live="polite">
+              {availablePlants}
+            </span>
+            <span className="hero-metric__label home__count-note">
+              Plants
+              <br />
+              Remaining
+            </span>
+          </div>
+
+          <h1 className="hero-section__headline home__headline" data-testid="hero-headline">
+            {content.home.heroTitle}
+          </h1>
+          <p className="hero-section__subhead home__subhead" data-testid="hero-subhead">
+            {content.home.heroSubtitle}
+          </p>
+          <Link
+            to="/shop"
+            className="hero-section__cta home__cta"
+            data-testid="hero-cta"
+          >
+            {content.home.shopCta}
+          </Link>
+        </div>
+
+        <div className="hero-section__bottom">
+          <span>{content.tagline}</span>
+          <span className="hero-section__scroll-cue">
+            Scroll<span className="hero-section__stem" />
+          </span>
+        </div>
       </header>
 
       <section className="paper-rip-section home__paper-rip" aria-label="Plant story scroll reveal">
-        <div className="paper-rip-section__inner">
+        <div className="paper-rip-section__reveal home__torn-reveal">
           <p className="paper-rip-section__eyebrow">Curated living spaces</p>
-          <h2>Thoughtful plants for work and home.</h2>
+          <h2>
+            Thoughtful plants, <em>freshly potted</em>
+          </h2>
+        </div>
+        <div className="paper-rip-section__half paper-rip-section__half--left home__paper-left">
+          <div className="paper-rip-section__grain">
+            <span className="paper-rip-section__stamp">Sealed<br />Crate</span>
+            <span>Handle with care</span>
+          </div>
+        </div>
+        <div className="paper-rip-section__half paper-rip-section__half--right home__paper-right">
+          <div className="paper-rip-section__grain">
+            <span>Greenhouse Row 4</span>
+          </div>
         </div>
       </section>
 
@@ -81,7 +114,9 @@ export function HomePage() {
         <div className="plant-grid">
           {plantCards.map((plant) => (
             <article key={plant.name} className="plant-card home__plant-card">
-              <img src={plant.image} alt={plant.name} />
+              <div className="plant-card__frame">
+                <img src={plant.image} alt={plant.name} loading="lazy" />
+              </div>
               <div className="plant-card__content">
                 <h3>{plant.name}</h3>
                 <p>{plant.price}</p>
@@ -100,12 +135,20 @@ export function HomePage() {
       </section>
 
       <section className="about-section home__about" aria-label="About QLeaves">
-        <p className="eyebrow">About</p>
-        <h2>We design room-by-room plant stories.</h2>
-        <p>
-          QLeaves sources resilient, design-led plants for homes and workspaces across
-          Qatar, blending calm greenery with practical care guidance.
-        </p>
+        <img
+          src="/images/hero/leaf-3.svg"
+          className="about-section__leaf"
+          alt=""
+          aria-hidden="true"
+        />
+        <div className="about-section__copy">
+          <p className="eyebrow">About</p>
+          <h2>We design room-by-room plant stories.</h2>
+          <p>
+            QLeaves sources resilient, design-led plants for homes and workspaces across
+            Qatar, blending calm greenery with practical care guidance.
+          </p>
+        </div>
       </section>
     </article>
   );
diff --git a/apps/web/src/features/home/home.css b/apps/web/src/features/home/home.css
index a26269e..28925bf 100644
--- a/apps/web/src/features/home/home.css
+++ b/apps/web/src/features/home/home.css
@@ -1,129 +1,297 @@
-/* QLeaves homepage editorial styles */
+/* QLeaves homepage — "Undergrowth" editorial system */
 .home {
   display: flex;
   flex-direction: column;
-  gap: clamp(3rem, 5vw, 5rem);
-  padding: 0 0 5rem;
+  gap: 0;
+  padding: 0;
 }
 
-.hero-section {
+/* ---------- HERO ---------- */
+.home__hero.hero-section {
   position: relative;
+  min-height: 100svh;
   display: flex;
   flex-direction: column;
+  align-items: stretch;
+  text-align: left;
+  gap: 0;
+  padding: 0;
+  overflow: hidden;
+  background: radial-gradient(120% 140% at 50% 0%, #1c2a1f 0%, var(--q-color-ink) 62%, #0e140f 100%);
+  color: var(--q-color-warm-ivory);
+}
+
+.hero-section__top {
+  position: relative;
+  z-index: 2;
+  display: flex;
+  justify-content: space-between;
   align-items: center;
-  text-align: center;
-  gap: var(--q-space-lg);
-  padding: clamp(4rem, 11vw, 7rem) 0 2rem;
+  padding: 1.75rem clamp(1.25rem, 5vw, 3.5rem) 0;
 }
 
-.hero-section__eyebrow,
-.eyebrow {
-  margin: 0;
-  font-size: 0.74rem;
-  letter-spacing: 0.2em;
+.hero-section__mark.home__wordmark {
+  font-family: var(--q-font-display);
+  font-style: italic;
+  font-weight: 600;
+  font-size: 1.15rem;
+}
+
+.hero-section__tag {
+  font-size: 0.72rem;
   text-transform: uppercase;
-  color: var(--q-color-forest-olive);
-  font-weight: 800;
+  letter-spacing: 0.16em;
+  color: rgba(242, 236, 217, 0.6);
 }
 
-.hero-metric {
-  display: inline-flex;
-  align-items: baseline;
+.hero-section__leaves {
+  position: absolute;
+  inset: 0;
+  z-index: 1;
+  pointer-events: none;
+  opacity: 0.5;
+}
+
+.hero-leaf {
+  position: absolute;
+  width: clamp(140px, 18vw, 260px);
+  filter: brightness(0.9);
+}
+
+.hero-leaf--1 {
+  top: 8%;
+  right: 8%;
+  transform: rotate(12deg);
+}
+
+.hero-leaf--2 {
+  bottom: 12%;
+  right: 22%;
+  width: clamp(100px, 12vw, 180px);
+  transform: rotate(-18deg);
+}
+
+.hero-leaf--3 {
+  top: 38%;
+  right: -2%;
+  width: clamp(90px, 10vw, 150px);
+  transform: rotate(30deg);
+}
+
+.hero-section__mid {
+  position: relative;
+  z-index: 2;
+  flex: 1;
+  display: flex;
+  flex-direction: column;
+  align-items: flex-start;
   justify-content: center;
-  gap: 0.8rem;
+  gap: var(--q-space-md);
+  padding: 0 clamp(1.25rem, 5vw, 3.5rem);
+}
+
+.hero-metric.home__count-wrap {
+  display: flex;
+  align-items: baseline;
+  gap: clamp(0.6rem, 2vw, 1.4rem);
   flex-wrap: wrap;
-  padding: 0.35rem 0;
 }
 
-.hero-metric__number {
-  display: inline-block;
+.hero-metric__number.home__count {
   font-family: var(--q-font-display);
-  font-size: clamp(4.25rem, 8vw, 9rem);
+  font-weight: 600;
+  font-size: clamp(4.5rem, 17vw, 10.5rem);
   line-height: 0.9;
-  font-weight: 900;
-  letter-spacing: -0.07em;
-  color: var(--q-color-forest-olive);
+  letter-spacing: -0.02em;
+  font-variant-numeric: tabular-nums;
+  color: var(--q-color-warm-ivory);
 }
 
-.hero-metric__label {
-  display: inline-block;
-  font-size: 0.72rem;
-  letter-spacing: 0.17em;
-  text-transform: uppercase;
+.hero-metric__label.home__count-note {
+  font-family: var(--q-font-body);
   font-weight: 800;
-  color: var(--q-color-forest-olive);
+  font-size: clamp(0.95rem, 2.4vw, 1.4rem);
+  line-height: 1.1;
+  text-transform: uppercase;
+  letter-spacing: 0.08em;
+  color: var(--q-color-terracotta);
+  padding-bottom: clamp(0.5rem, 2vw, 1.1rem);
 }
 
-.hero-section__headline {
-  font-size: clamp(3.2rem, 6.2vw, 6.2rem);
-  line-height: 0.9;
-  max-width: 42rem;
+.hero-section__headline.home__headline {
+  font-size: clamp(2.4rem, 5.4vw, 4.6rem);
+  line-height: 1;
+  max-width: 20ch;
   margin: 0;
-  font-weight: 900;
-  letter-spacing: -0.07em;
+  font-weight: 500;
+  letter-spacing: -0.02em;
+  color: var(--q-color-warm-ivory);
 }
 
-.hero-section__subhead {
-  font-size: clamp(1.05rem, 1.8vw, 1.4rem);
-  color: rgba(31, 46, 36, 0.7);
-  max-width: 36rem;
+.hero-section__subhead.home__subhead {
+  font-size: clamp(1rem, 1.6vw, 1.15rem);
+  color: rgba(242, 236, 217, 0.78);
+  max-width: 34ch;
   margin: 0;
-  font-weight: 600;
+  font-weight: 400;
+  line-height: 1.55;
 }
 
-.hero-section__cta {
+.hero-section__cta.home__cta {
   display: inline-flex;
   align-items: center;
   justify-content: center;
   gap: var(--q-space-xs);
-  padding: 1rem 1.6rem;
-  background: linear-gradient(135deg, var(--q-color-forest-olive), #7b9a77);
+  margin-top: 0.5rem;
+  padding: 0.9rem 1.6rem;
+  background: var(--q-color-terracotta);
   color: var(--q-color-warm-ivory);
-  border-radius: var(--q-radius-full);
-  font-weight: 800;
+  font-weight: 700;
   letter-spacing: 0.02em;
-  box-shadow: none;
 }
 
-.paper-rip-section {
+.hero-section__bottom {
   position: relative;
-  min-height: 24rem;
-  display: grid;
-  place-items: center;
-  background:
-    linear-gradient(135deg, rgba(29, 44, 34, 0.9), rgba(106, 138, 103, 0.76)),
-    url("https://images.unsplash.com/photo-1466692476868-aef1dfb1e735?auto=format&fit=crop&w=1200&q=80") center/cover no-repeat;
-  clip-path: polygon(0 0, 100% 0, 100% 95%, 92% 100%, 79% 96%, 66% 100%, 52% 94%, 38% 100%, 24% 96%, 0 100%);
+  z-index: 2;
+  display: flex;
+  justify-content: space-between;
+  align-items: flex-end;
+  padding: 0 clamp(1.25rem, 5vw, 3.5rem) 1.9rem;
+  font-size: 0.75rem;
+  text-transform: uppercase;
+  letter-spacing: 0.14em;
+  color: rgba(242, 236, 217, 0.55);
+}
+
+.hero-section__scroll-cue {
+  display: flex;
+  align-items: center;
+  gap: 10px;
+}
+
+.hero-section__stem {
+  width: 1px;
+  height: 34px;
+  background: rgba(242, 236, 217, 0.45);
+}
+
+/* ---------- PAPER RIP ---------- */
+.paper-rip-section.home__paper-rip {
+  position: relative;
+  min-height: 100svh;
+  display: flex;
+  align-items: center;
+  justify-content: center;
   overflow: hidden;
-  border-radius: 1.5rem;
+  background: radial-gradient(90% 120% at 50% 20%, #40603f 0%, var(--q-color-ink-deep) 70%);
+  border-radius: 0;
   box-shadow: none;
+  clip-path: none;
 }
 
-.paper-rip-section__inner {
-  width: min(42rem, 80%);
+.paper-rip-section__reveal.home__torn-reveal {
+  position: absolute;
+  inset: 0;
+  z-index: 1;
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  justify-content: center;
   text-align: center;
-  color: var(--q-color-warm-ivory);
+  padding: 0 1.5rem;
 }
 
-.paper-rip-section__inner h2 {
-  font-size: clamp(2.2rem, 4vw, 4rem);
-  margin: 0.4rem 0 0;
-  font-weight: 900;
-  letter-spacing: -0.05em;
+.paper-rip-section__eyebrow {
+  margin: 0 0 0.9rem;
+  font-size: 0.75rem;
+  letter-spacing: 0.22em;
+  text-transform: uppercase;
+  color: var(--q-color-soft-sand);
+  opacity: 0.85;
 }
 
-.paper-rip-section__eyebrow {
+.paper-rip-section__reveal h2 {
+  font-family: var(--q-font-display);
+  font-weight: 500;
+  color: var(--q-color-warm-ivory);
+  font-size: clamp(2rem, 5.6vw, 4.2rem);
+  line-height: 1.06;
+  max-width: 16ch;
   margin: 0;
-  letter-spacing: 0.16em;
+}
+
+.paper-rip-section__reveal em {
+  font-style: italic;
+  color: var(--q-color-gold);
+}
+
+.paper-rip-section__half {
+  position: absolute;
+  top: -4%;
+  z-index: 2;
+  width: 54%;
+  height: 108%;
+  background:
+    repeating-linear-gradient(0deg, rgba(22, 31, 24, 0.035) 0 2px, transparent 2px 4px),
+    linear-gradient(160deg, var(--q-color-warm-ivory) 0%, var(--q-color-soft-sand) 100%);
+  box-shadow: var(--q-shadow-lg);
+  will-change: transform, opacity;
+}
+
+.paper-rip-section__half--left {
+  left: -2%;
+  clip-path: polygon(0 0, 100% 0, 86% 6%, 91% 12%, 83% 19%, 89% 27%, 80% 35%, 88% 44%, 79% 53%, 90% 62%, 82% 71%, 91% 80%, 84% 90%, 92% 100%, 0 100%);
+}
+
+.paper-rip-section__half--right {
+  right: -2%;
+  clip-path: polygon(14% 0, 100% 0, 100% 100%, 8% 100%, 16% 90%, 9% 80%, 18% 71%, 10% 62%, 21% 53%, 12% 44%, 20% 35%, 11% 27%, 17% 19%, 9% 12%, 14% 6%);
+}
+
+.paper-rip-section__grain {
+  position: absolute;
+  inset: 0;
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  justify-content: center;
+  gap: 10px;
+  opacity: 0.55;
+}
+
+.paper-rip-section__grain span {
+  font-family: var(--q-font-body);
+  font-weight: 700;
+  letter-spacing: 0.3em;
   text-transform: uppercase;
-  font-size: 0.72rem;
-  opacity: 0.9;
+  font-size: 0.68rem;
+  color: var(--q-color-ink-deep);
 }
 
-.plant-gallery {
+.paper-rip-section__stamp {
+  width: 64px;
+  height: 64px;
+  border: 2px solid var(--q-color-terracotta);
+  border-radius: 50%;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  color: var(--q-color-terracotta);
+  font-family: var(--q-font-display);
+  font-style: italic;
+  font-size: 0.7rem;
+  line-height: 1.1;
+  transform: rotate(-14deg);
+  opacity: 0.75;
+  text-align: center;
+}
+
+/* ---------- PLANT GALLERY ---------- */
+.plant-gallery.home__plant-gallery {
   display: flex;
   flex-direction: column;
   gap: var(--q-space-xl);
+  padding: clamp(4rem, 8vw, 6.5rem) clamp(1.25rem, 5vw, 3.5rem);
   opacity: 0;
   transform: translateY(2.5rem);
 }
@@ -131,109 +299,162 @@
 .plant-gallery__header {
   display: flex;
   flex-direction: column;
-  gap: 0.4rem;
+  gap: 0.6rem;
   text-align: left;
+  border-bottom: 1px solid var(--q-color-border);
+  padding-bottom: 1.5rem;
 }
 
 .plant-gallery__header h2 {
-  font-size: clamp(2rem, 3.5vw, 3.2rem);
+  font-size: clamp(2rem, 3.5vw, 3rem);
   margin: 0;
-  font-weight: 900;
-  letter-spacing: -0.05em;
+  font-weight: 500;
+  max-width: 14ch;
 }
 
 .plant-grid {
   display: grid;
-  grid-template-columns: repeat(auto-fit, minmax(15rem, 1fr));
-  gap: var(--q-space-xl);
+  grid-template-columns: repeat(4, 1fr);
+  gap: 2px;
+  background: var(--q-color-border);
+  border: 1px solid var(--q-color-border);
+}
+
+@media (max-width: 900px) {
+  .plant-grid {
+    grid-template-columns: repeat(2, 1fr);
+  }
+}
+
+@media (max-width: 520px) {
+  .plant-grid {
+    grid-template-columns: 1fr;
+  }
 }
 
 .plant-card {
+  background: var(--q-color-background);
+  padding: 1.1rem 1.1rem 1.3rem;
+  display: flex;
+  flex-direction: column;
+}
+
+.plant-card__frame {
+  aspect-ratio: 4 / 5;
   overflow: hidden;
-  background: rgba(255, 255, 255, 0.34);
-  border: 1px solid rgba(29, 44, 34, 0.08);
-  border-radius: 1.25rem;
-  box-shadow: none;
+  background: var(--q-color-soft-sand);
+  margin-bottom: 1rem;
 }
 
-.plant-card img {
+.plant-card__frame img {
   width: 100%;
-  height: 18.5rem;
+  height: 100%;
   object-fit: cover;
-  display: block;
+  transition: transform 1.1s cubic-bezier(0.16, 0.84, 0.44, 1);
+}
+
+.plant-card:hover .plant-card__frame img {
+  transform: scale(1.06);
 }
 
 .plant-card__content {
   display: flex;
   justify-content: space-between;
-  align-items: center;
+  align-items: baseline;
   gap: 1rem;
-  padding: 1rem 1.1rem 1.2rem;
+  padding-top: 0.85rem;
+  border-top: 1px solid var(--q-color-border);
 }
 
 .plant-card__content h3 {
   margin: 0;
-  font-size: 1.1rem;
-  font-weight: 800;
+  font-size: 1.05rem;
+  font-weight: 500;
 }
 
 .plant-card__content p {
   margin: 0;
-  font-size: 0.9rem;
+  font-size: 0.92rem;
   font-weight: 700;
+  font-family: var(--q-font-display);
   color: var(--q-color-forest-olive);
 }
 
+/* ---------- SHOP CTA ---------- */
 .shop-cta {
   display: flex;
   flex-direction: column;
   align-items: center;
   gap: var(--q-space-md);
   text-align: center;
-  padding: var(--q-space-3xl) 0 0;
+  padding: clamp(4rem, 8vw, 6.5rem) 1.5rem;
+  background: var(--q-color-moss);
+  color: var(--q-color-warm-ivory);
 }
 
 .shop-cta__title {
-  font-size: clamp(2.2rem, 4vw, 3.2rem);
+  font-size: clamp(2rem, 4vw, 3rem);
   margin: 0;
-  font-weight: 900;
-  letter-spacing: -0.05em;
+  font-weight: 500;
 }
 
 .shop-cta__message {
   margin: 0;
-  color: rgba(31, 46, 36, 0.72);
+  color: rgba(242, 236, 217, 0.8);
 }
 
 .shop-cta__link {
-  color: var(--q-color-forest-olive);
-  font-weight: 800;
+  color: var(--q-color-warm-ivory);
+  font-weight: 700;
+  text-decoration: underline;
+  text-underline-offset: 4px;
 }
 
-.about-section {
+/* ---------- ABOUT (last) ---------- */
+.about-section.home__about {
+  position: relative;
   display: flex;
   flex-direction: column;
   gap: 0.9rem;
-  padding-top: var(--q-space-xl);
-  border-top: 1px solid rgba(31, 46, 36, 0.12);
+  padding: clamp(4rem, 8vw, 6.5rem) clamp(1.25rem, 5vw, 3.5rem) clamp(5rem, 9vw, 7.5rem);
+  overflow: hidden;
+}
+
+.about-section__leaf {
+  position: absolute;
+  top: -4rem;
+  right: -3rem;
+  width: clamp(160px, 22vw, 320px);
+  opacity: 0.12;
+  transform: rotate(-8deg);
+  pointer-events: none;
+}
+
+.about-section__copy {
+  position: relative;
+  display: flex;
+  flex-direction: column;
+  gap: 0.9rem;
+  max-width: 48rem;
 }
 
 .about-section h2 {
   margin: 0;
-  font-size: clamp(2.1rem, 3vw, 3rem);
-  font-weight: 900;
-  letter-spacing: -0.05em;
+  font-size: clamp(2rem, 3vw, 2.8rem);
+  font-weight: 500;
 }
 
 .about-section p {
   margin: 0;
-  color: rgba(31, 46, 36, 0.7);
-  max-width: 48rem;
+  color: rgba(22, 31, 24, 0.72);
+  max-width: 48ch;
 }
 
 [data-motion="reduced"] .home__fragment,
 [data-motion="reduced"] .home__featured,
-[data-motion="reduced"] .home__plant-gallery {
+[data-motion="reduced"] .home__plant-gallery,
+[data-motion="reduced"] .home__paper-left,
+[data-motion="reduced"] .home__paper-right {
   opacity: 1;
   transform: none;
 }
diff --git a/apps/web/src/features/home/useHeroMotion.ts b/apps/web/src/features/home/useHeroMotion.ts
index a2ecffa..758cc97 100644
--- a/apps/web/src/features/home/useHeroMotion.ts
+++ b/apps/web/src/features/home/useHeroMotion.ts
@@ -33,40 +33,58 @@ export function useHeroMotion(root: RefObject<HTMLElement | null>): void {
 
       intro
         .from(".home__wordmark", { opacity: 0, y: 26, duration: 0.8 })
+        .from(".hero-section__tag", { opacity: 0, y: 16, duration: 0.7 }, "-=0.55")
         .from(".home__count", { opacity: 0, y: 54, duration: 1.1 }, "-=0.4")
         .from(".home__count-note", { opacity: 0, x: 24, duration: 0.7 }, "-=0.55")
         .from(".home__headline", { opacity: 0, y: 18, duration: 0.9 }, "-=0.6")
         .from(".home__subhead", { opacity: 0, y: 12, duration: 0.7 }, "-=0.45")
         .from(".home__cta", { opacity: 0, y: 10, duration: 0.7 }, "-=0.35");
 
-      const paperScroll = gsap.timeline({
-        scrollTrigger: {
-          trigger: ".home__paper-rip",
-          start: "top 72%",
-          end: "bottom 18%",
-          scrub: true,
-        },
-      });
-
-      paperScroll.to(".home__paper-rip", {
-        clipPath: "polygon(0 0, 100% 0, 100% 95%, 92% 100%, 79% 96%, 66% 100%, 52% 94%, 38% 100%, 24% 96%, 0 100%)",
-        ease: "none",
+      // Ambient hero leaves — a lightweight stand-in for a particle field,
+      // reusing the SVGs already committed under public/images/hero instead
+      // of pulling in three.js just for a decorative drift.
+      gsap.utils.toArray<HTMLElement>(".hero-leaf").forEach((leaf, i) => {
+        gsap.to(leaf, {
+          y: "+=18",
+          rotate: "+=6",
+          duration: 4 + i,
+          ease: "sine.inOut",
+          yoyo: true,
+          repeat: -1,
+        });
       });
 
-      const paperReveal = gsap.timeline({
+      // Scroll-scrubbed paper tear: the section pins for one viewport's worth
+      // of scroll while the two paper halves are dragged apart in lockstep
+      // with scroll position (scrub: true ties the timeline directly to
+      // scroll progress, GSAP's equivalent of manually seeking a timeline).
+      const paperScroll = gsap.timeline({
         scrollTrigger: {
           trigger: ".home__paper-rip",
-          start: "top 80%",
-          once: true,
+          start: "top top",
+          end: "+=120%",
+          scrub: true,
+          pin: true,
         },
       });
 
-      paperReveal.from(".home__paper-rip", {
-        opacity: 0.3,
-        y: 20,
-        duration: 0.9,
-        ease: "power2.out",
-      });
+      paperScroll
+        .to(
+          ".home__paper-left",
+          { xPercent: -120, yPercent: -16, rotate: -16, opacity: 0, ease: "none" },
+          0,
+        )
+        .to(
+          ".home__paper-right",
+          { xPercent: 120, yPercent: 14, rotate: 16, opacity: 0, ease: "none" },
+          0,
+        )
+        .fromTo(
+          ".home__torn-reveal",
+          { opacity: 0.15, scale: 0.94 },
+          { opacity: 1, scale: 1, ease: "none" },
+          0,
+        );
 
       const gallery = gsap.timeline({
         scrollTrigger: {
diff --git a/apps/web/src/styles/global.css b/apps/web/src/styles/global.css
index 1b41185..14774a0 100644
--- a/apps/web/src/styles/global.css
+++ b/apps/web/src/styles/global.css
@@ -23,10 +23,7 @@ body {
   margin: 0;
   font-family: var(--q-font-body);
   font-size: var(--q-font-size-base);
-  background:
-    radial-gradient(circle at top, rgba(113, 148, 104, 0.36), transparent 28%),
-    radial-gradient(circle at 80% 12%, rgba(185, 109, 70, 0.18), transparent 20%),
-    linear-gradient(180deg, #edf0d8 0%, #e7d8b5 26%, #f6efe6 100%);
+  background: var(--q-color-background);
   color: var(--q-color-on-background);
   line-height: 1.5;
   -webkit-tap-highlight-color: transparent;
@@ -41,9 +38,9 @@ h5,
 h6 {
   margin: 0;
   font-family: var(--q-font-display);
-  font-weight: 400;
+  font-weight: 500;
   line-height: 1.1;
-  letter-spacing: -0.02em;
+  letter-spacing: -0.01em;
 }
 
 p {
@@ -130,8 +127,8 @@ canvas {
   position: sticky;
   top: 0;
   z-index: 10;
-  background: linear-gradient(180deg, rgba(247, 242, 234, 0.96), rgba(235, 223, 202, 0.94));
-  border-bottom: 1px solid rgba(29, 44, 34, 0.08);
+  background: var(--q-color-background);
+  border-bottom: 1px solid var(--q-color-border);
 }
 
 .site-header__inner {
@@ -147,6 +144,8 @@ canvas {
   display: inline-flex;
   align-items: center;
   font-family: var(--q-font-display);
+  font-style: italic;
+  font-weight: 600;
   font-size: var(--q-font-size-xl);
   color: var(--q-color-on-background);
 }
@@ -158,9 +157,11 @@ canvas {
 }
 
 .nav-link {
-  color: var(--q-color-moss);
+  color: var(--q-color-forest-olive);
   font-size: var(--q-font-size-sm);
   font-weight: 600;
+  text-transform: uppercase;
+  letter-spacing: 0.08em;
 }
 
 .nav-link[aria-current="page"],
@@ -175,13 +176,17 @@ canvas {
 }
 
 .site-footer {
-  border-top: 1px solid rgba(29, 44, 34, 0.08);
+  margin: 0;
+  padding: var(--q-space-lg) var(--q-space-md);
+  color: rgba(242, 236, 217, 0.7);
+  font-size: var(--q-font-size-sm);
+  background: var(--q-color-ink);
+}
+
+.site-footer small {
+  display: block;
   max-width: 72rem;
   margin: 0 auto;
-  padding: var(--q-space-md);
-  color: var(--q-color-forest-olive);
-  font-size: var(--q-font-size-sm);
-  background: linear-gradient(180deg, rgba(255, 255, 255, 0), rgba(233, 214, 185, 0.26));
 }
 
 .page-shell {
@@ -199,9 +204,10 @@ canvas {
 
 .eyebrow {
   margin: 0;
-  color: var(--q-color-sage);
+  color: var(--q-color-terracotta);
   font-size: var(--q-font-size-sm);
-  letter-spacing: 0.12em;
+  font-weight: 700;
+  letter-spacing: 0.16em;
   text-transform: uppercase;
 }
 
diff --git a/apps/web/src/styles/tokens.css b/apps/web/src/styles/tokens.css
index 91c85bc..6dc9c68 100644
--- a/apps/web/src/styles/tokens.css
+++ b/apps/web/src/styles/tokens.css
@@ -1,35 +1,38 @@
-@import url("https://fonts.googleapis.com/css2?family=Archivo:wght@400;500;600;700;800;900&family=Manrope:wght@400;500;600;700;800&display=swap");
+@import url("https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,300;0,9..144,450;0,9..144,600;0,9..144,700;1,9..144,450&family=Archivo:wght@400;500;600;700;800;900&display=swap");
 
-/* QLeaves design tokens. Tuned toward an earthy, minimal editorial storefront. */
+/* QLeaves design tokens — "Undergrowth" system: torn-paper editorial, kraft palette. */
 :root {
-  /* Deep earthy forest */
-  --q-color-deep-olive: #1d2c22;
+  /* Deep earthy forest (near-black ink) */
+  --q-color-deep-olive: #161f18;
   /* Muted olive for panels */
-  --q-color-forest-olive: #415d4a;
+  --q-color-forest-olive: #3f5b43;
   /* Nature green */
-  --q-color-moss: #6a8a67;
+  --q-color-moss: #3f5b43;
   /* Calm sage */
   --q-color-sage: #bacbb3;
   /* Warm parchment */
-  --q-color-warm-ivory: #f7f2ea;
+  --q-color-warm-ivory: #f2ecd9;
   /* Soft sand */
-  --q-color-soft-sand: #e8dcc0;
+  --q-color-soft-sand: #ddd3ae;
   /* Clay accent */
-  --q-color-terracotta: #b96d46;
+  --q-color-terracotta: #b0603a;
   /* Muted gold */
-  --q-color-gold: #caa86a;
+  --q-color-gold: #d7c58a;
 
   /* Derived UI surfaces */
   --q-color-background: var(--q-color-warm-ivory);
-  --q-color-surface: #fbf8f2;
+  --q-color-surface: #e9e0c6;
   --q-color-on-background: var(--q-color-deep-olive);
   --q-color-on-surface: var(--q-color-deep-olive);
-  --q-color-border: rgba(29, 44, 34, 0.1);
+  --q-color-border: rgba(22, 31, 24, 0.14);
   --q-color-accent: var(--q-color-terracotta);
+  /* Near-black ink surface, used for the hero and other dark panels */
+  --q-color-ink: #161f18;
+  --q-color-ink-deep: #28382b;
 
-  /* Typography */
-  --q-font-display: "Archivo", "Arial Black", sans-serif;
-  --q-font-body: "Manrope", "Segoe UI", sans-serif;
+  /* Typography — serif display paired with one grotesk workhorse, no third face */
+  --q-font-display: "Fraunces", serif;
+  --q-font-body: "Archivo", "Segoe UI", sans-serif;
   --q-font-size-xs: 0.75rem;
   --q-font-size-sm: 0.875rem;
   --q-font-size-base: 1rem;
@@ -54,17 +57,17 @@
   --q-space-3xl: 3rem;
   --q-space-4xl: 4rem;
 
-  /* Border radius */
-  --q-radius-sm: 0.25rem;
-  --q-radius-md: 0.5rem;
-  --q-radius-lg: 0.75rem;
-  --q-radius-xl: 1rem;
-  --q-radius-full: 9999px;
+  /* Border radius — the Undergrowth system is sharp-cornered/cut-paper, not pill/soft */
+  --q-radius-sm: 0;
+  --q-radius-md: 0;
+  --q-radius-lg: 0;
+  --q-radius-xl: 0;
+  --q-radius-full: 0;
 
   /* Shadows */
-  --q-shadow-sm: 0 1px 3px rgba(31, 46, 36, 0.08);
-  --q-shadow-md: 0 8px 18px rgba(31, 46, 36, 0.08);
-  --q-shadow-lg: 0 18px 35px rgba(31, 46, 36, 0.12);
+  --q-shadow-sm: 0 1px 3px rgba(22, 31, 24, 0.08);
+  --q-shadow-md: 0 8px 18px rgba(22, 31, 24, 0.08);
+  --q-shadow-lg: 0 24px 48px rgba(22, 31, 24, 0.28);
 }
 
 @media (prefers-color-scheme: light) {
diff --git a/package-lock.json b/package-lock.json
index d052497..841c07e 100644
--- a/package-lock.json
+++ b/package-lock.json
@@ -20,11 +20,13 @@
       "dependencies": {
         "@prisma/client": "6.16.2",
         "argon2": "0.44.0",
+        "compression": "^1.7.4",
         "express": "^5.1.0",
         "zod": "^3.24.1"
       },
       "devDependencies": {
-        "@types/express": "^5.0.3",
+        "@types/compression": "^1.8.1",
+        "@types/express": "^5.0.6",
         "@types/node": "^24.3.0",
         "@types/supertest": "^6.0.3",
         "prisma": "6.16.2",
@@ -1012,9 +1014,6 @@
         "x64"
       ],
       "dev": true,
-      "libc": [
-        "glibc"
-      ],
       "license": "MIT",
       "optional": true,
       "os": [
@@ -1264,9 +1263,6 @@
         "arm"
       ],
       "dev": true,
-      "libc": [
-        "glibc"
-      ],
       "license": "MIT",
       "optional": true,
       "os": [
@@ -1281,9 +1277,6 @@
         "arm"
       ],
       "dev": true,
-      "libc": [
-        "musl"
-      ],
       "license": "MIT",
       "optional": true,
       "os": [
@@ -1298,9 +1291,6 @@
         "arm64"
       ],
       "dev": true,
-      "libc": [
-        "glibc"
-      ],
       "license": "MIT",
       "optional": true,
       "os": [
@@ -1315,9 +1305,6 @@
         "arm64"
       ],
       "dev": true,
-      "libc": [
-        "musl"
-      ],
       "license": "MIT",
       "optional": true,
       "os": [
@@ -1332,9 +1319,6 @@
         "loong64"
       ],
       "dev": true,
-      "libc": [
-        "glibc"
-      ],
       "license": "MIT",
       "optional": true,
       "os": [
@@ -1349,9 +1333,6 @@
         "loong64"
       ],
       "dev": true,
-      "libc": [
-        "musl"
-      ],
       "license": "MIT",
       "optional": true,
       "os": [
@@ -1366,9 +1347,6 @@
         "ppc64"
       ],
       "dev": true,
-      "libc": [
-        "glibc"
-      ],
       "license": "MIT",
       "optional": true,
       "os": [
@@ -1383,9 +1361,6 @@
         "ppc64"
       ],
       "dev": true,
-      "libc": [
-        "musl"
-      ],
       "license": "MIT",
       "optional": true,
       "os": [
@@ -1400,9 +1375,6 @@
         "riscv64"
       ],
       "dev": true,
-      "libc": [
-        "glibc"
-      ],
       "license": "MIT",
       "optional": true,
       "os": [
@@ -1417,9 +1389,6 @@
         "riscv64"
       ],
       "dev": true,
-      "libc": [
-        "musl"
-      ],
       "license": "MIT",
       "optional": true,
       "os": [
@@ -1434,9 +1403,6 @@
         "s390x"
       ],
       "dev": true,
-      "libc": [
-        "glibc"
-      ],
       "license": "MIT",
       "optional": true,
       "os": [
@@ -1451,9 +1417,6 @@
         "x64"
       ],
       "dev": true,
-      "libc": [
-        "glibc"
-      ],
       "license": "MIT",
       "optional": true,
       "os": [
@@ -1468,9 +1431,6 @@
         "x64"
       ],
       "dev": true,
-      "libc": [
-        "musl"
-      ],
       "license": "MIT",
       "optional": true,
       "os": [
@@ -1759,6 +1719,17 @@
         "assertion-error": "^2.0.1"
       }
     },
+    "node_modules/@types/compression": {
+      "version": "1.8.1",
+      "resolved": "https://registry.npmjs.org/@types/compression/-/compression-1.8.1.tgz",
+      "integrity": "sha512-kCFuWS0ebDbmxs0AXYn6e2r2nrGAb5KwQhknjSPSPgJcGd8+HVSILlUyFhGqML2gk39HcG7D1ydW9/qpYkN00Q==",
+      "dev": true,
+      "license": "MIT",
+      "dependencies": {
+        "@types/express": "*",
+        "@types/node": "*"
+      }
+    },
     "node_modules/@types/connect": {
       "version": "3.4.38",
       "resolved": "https://registry.npmjs.org/@types/connect/-/connect-3.4.38.tgz",
@@ -2476,6 +2447,60 @@
         "url": "https://github.com/sponsors/sindresorhus"
       }
     },
+    "node_modules/compressible": {
+      "version": "2.0.18",
+      "resolved": "https://registry.npmjs.org/compressible/-/compressible-2.0.18.tgz",
+      "integrity": "sha512-AF3r7P5dWxL8MxyITRMlORQNaOA2IkAFaTr4k7BUumjPtRpGDTZpl0Pb1XCO6JeDCBdp126Cgs9sMxqSjgYyRg==",
+      "license": "MIT",
+      "dependencies": {
+        "mime-db": ">= 1.43.0 < 2"
+      },
+      "engines": {
+        "node": ">= 0.6"
+      }
+    },
+    "node_modules/compression": {
+      "version": "1.8.1",
+      "resolved": "https://registry.npmjs.org/compression/-/compression-1.8.1.tgz",
+      "integrity": "sha512-9mAqGPHLakhCLeNyxPkK4xVo746zQ/czLH1Ky+vkitMnWfWZps8r0qXuwhwizagCRttsL4lfG4pIOvaWLpAP0w==",
+      "license": "MIT",
+      "dependencies": {
+        "bytes": "3.1.2",
+        "compressible": "~2.0.18",
+        "debug": "2.6.9",
+        "negotiator": "~0.6.4",
+        "on-headers": "~1.1.0",
+        "safe-buffer": "5.2.1",
+        "vary": "~1.1.2"
+      },
+      "engines": {
+        "node": ">= 0.8.0"
+      }
+    },
+    "node_modules/compression/node_modules/debug": {
+      "version": "2.6.9",
+      "resolved": "https://registry.npmjs.org/debug/-/debug-2.6.9.tgz",
+      "integrity": "sha512-bC7ElrdJaJnPbAP+1EotYvqZsb3ecl5wi6Bfi6BJTUcNowp6cvspg0jXznRTKDjm/E7AdgFBVeAPVMNcKGsHMA==",
+      "license": "MIT",
+      "dependencies": {
+        "ms": "2.0.0"
+      }
+    },
+    "node_modules/compression/node_modules/ms": {
+      "version": "2.0.0",
+      "resolved": "https://registry.npmjs.org/ms/-/ms-2.0.0.tgz",
+      "integrity": "sha512-Tpp60P6IUJDTuOq/5Z8cdskzJujfwqfOTkrwIwj7IRISpnkJnT6SyJ4PCPnGMoFjC9ddhal5KVIYtAt97ix05A==",
+      "license": "MIT"
+    },
+    "node_modules/compression/node_modules/negotiator": {
+      "version": "0.6.4",
+      "resolved": "https://registry.npmjs.org/negotiator/-/negotiator-0.6.4.tgz",
+      "integrity": "sha512-myRT3DiWPHqho5PrJaIRyaMv2kgYf0mUVgBNOYMuCH5Ki1yEiQaf/ZJuQ62nvpc44wL5WDbTX7yGJi1Neevw8w==",
+      "license": "MIT",
+      "engines": {
+        "node": ">= 0.6"
+      }
+    },
     "node_modules/concurrently": {
       "version": "9.2.4",
       "resolved": "https://registry.npmjs.org/concurrently/-/concurrently-9.2.4.tgz",
@@ -3851,6 +3876,15 @@
         "node": ">= 0.8"
       }
     },
+    "node_modules/on-headers": {
+      "version": "1.1.0",
+      "resolved": "https://registry.npmjs.org/on-headers/-/on-headers-1.1.0.tgz",
+      "integrity": "sha512-737ZY3yNnXy37FHkQxPzt4UZ2UWPWiCZWLvFZ4fu5cueciegX0zGPnrlY6bwRg4FdQOe9YU8MkmJwGhoMybl8A==",
+      "license": "MIT",
+      "engines": {
+        "node": ">= 0.8"
+      }
+    },
     "node_modules/once": {
       "version": "1.4.0",
       "resolved": "https://registry.npmjs.org/once/-/once-1.4.0.tgz",
@@ -4414,6 +4448,26 @@
         "tslib": "^2.1.0"
       }
     },
+    "node_modules/safe-buffer": {
+      "version": "5.2.1",
+      "resolved": "https://registry.npmjs.org/safe-buffer/-/safe-buffer-5.2.1.tgz",
+      "integrity": "sha512-rp3So07KcdmmKbGvgaNxQSJr7bGVSVk5S9Eq1F+ppbRo70+YeaDxkw5Dd8NPN+GD6bjnYm2VuPuCXmpuYvmCXQ==",
+      "funding": [
+        {
+          "type": "github",
+          "url": "https://github.com/sponsors/feross"
+        },
+        {
+          "type": "patreon",
+          "url": "https://www.patreon.com/feross"
+        },
+        {
+          "type": "consulting",
+          "url": "https://feross.org/support"
+        }
+      ],
+      "license": "MIT"
+    },
     "node_modules/safer-buffer": {
       "version": "2.1.2",
       "resolved": "https://registry.npmjs.org/safer-buffer/-/safer-buffer-2.1.2.tgz",
